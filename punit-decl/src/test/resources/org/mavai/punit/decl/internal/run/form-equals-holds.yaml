format: mavai-contract/1
contract: form-equals-holds
service: form-oracle
transforms:
  doc: json
criteria:
  - name: the-form-holds
    threshold: 0.9
    postconditions:
      - parses: doc
      - path: "$.status"
        equals: "approved"
inputs:
  - "judge the document"
  - "judge it again"
