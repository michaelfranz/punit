format: mavai-contract/1
contract: form-not-equals-holds
service: form-oracle
transforms:
  doc: json
criteria:
  - name: the-form-holds
    threshold: 0.9
    postconditions:
      - parses: doc
      - path: "$.status"
        not-equals: "declined"
inputs:
  - "judge the document"
  - "judge it again"
