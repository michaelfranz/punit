format: mavai-contract/1
contract: form-matches-fails
service: form-oracle
transforms:
  doc: json
criteria:
  - name: the-form-holds
    threshold: 0.9
    postconditions:
      - parses: doc
      - matches: '^[0-9]+$'
inputs:
  - "judge the document"
  - "judge it again"
